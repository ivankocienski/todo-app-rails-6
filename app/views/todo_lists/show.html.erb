<h1><%= @todo_list.title %></h1>

<% if @todo_list.aspiration %>
<h2><%= link_to @todo_list.aspiration.title, aspiration_path(@todo_list.aspiration) %></h2>
<% end %>

<p><%= link_to 'Edit', edit_todo_path(@todo_list) %></p>

<% if @show_only_pending %>
    <p>Showing only pending items</p>

<% else %>
    <p><a href="?filter=pending">Show only pending items</a></p>
<% end %>

<ul class='index' id='todo-items'>
<% @todo_items.each do |item| %>
    <li><%= link_to todo_item_title_status(item), todo_item_path(@todo_list, item) %></li>
<% end %>
</ul>

<%= link_to 'Add New Item...', new_todo_item_path(@todo_list) %>